# Create a tree of dicts from regents.csv
# Print the contents of the tree with indentation for each level
# Using the tree (and not the rows), count the number of graduate students and print the result
